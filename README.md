# BEC

Bot Telegram para extração de vídeos e áudios do YouTube e Instagram e conversão de tipo.

## 🚀 Como rodar

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```
